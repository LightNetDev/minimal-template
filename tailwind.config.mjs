import { lightnetStyles } from "lightnet/tailwind.config.ts";
/**
 * @type {import('tailwindcss').Config}
 */
export default {
  presets: [
    lightnetStyles({
      primaryColor: "#1F2035",
    }),
  ],
};
